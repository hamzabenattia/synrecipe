<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{path('app_home')}}">Navbar</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link active" href="{{path('app_home')}}">Home
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link active" href="{{path('app_ingredient')}}">Mes Ingredient
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link active" href="{{path('recettes.index')}}">Mes Recettes
					</a>
				</li>
			</ul>
			<div class="d-flex">
				<ul class="navbar-nav me-auto">


					{% if app.user %}
						<li class="nav-item dropdown" style="margin-right: 5rem;">
							<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{app.user.fullName}}</a>
							<div class="dropdown-menu">
								<a class="dropdown-item" href="{{path('user_edit')}}">Modifier profile</a>
								<a class="dropdown-item" href="{{path('user_change_password')}}">Modifier le mots de passe</a>
								<a class="dropdown-item" href="{{path('app_logout')}}">Deconnexion</a>

							</div>


						</li>


					{% else %}
						<a href="{{path('app_login')}}" class="btn btn-secondary my-2 my-sm-0">Connexion</a>
					{% endif %}

				</div>
			</div>
		</div>
	</nav>
