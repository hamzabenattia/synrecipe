{% extends 'base.html.twig' %}

{% block title %}Ajouter une Recette
{% endblock %}

{% block body %}

	<div class="container">
    
		<h1 class="mt-4">Création une nouvelle Recette</h1>

{% for message in app.flashes('error') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}
		
       {{ form_start(form) }}
       
            {{ form_widget(form) }}

            <button type="submit" class="btn btn-primary mt-4">
           Créer la recette
        </button>

       {{ form_end(form) }}


		

	{% endblock %}

